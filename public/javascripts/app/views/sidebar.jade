.navbar-default.navbar-static-side

  .sidebar-space

  .sidebar-collapse
    h5 Utilities
    ul.utilities#side-menu.nav
      li
        a(href="#/key_finder")
          span.fa.fa-key
          | Key Finder

  .sidebar-space

  .sidebar-collapse
    h5 Models
    ul#side-menu.nav
      - for name in models
        li
          a.record(href="#/record/#{name}")
            span.fa.fa-table
            | #{name}
            .label.label-default(class="count-#{name}")
              | #{count[name]}
          a.schema(href="#/schema/#{name}")
            span.fa.fa-chevron-right

  .sidebar-space

  .sidebar-collapse
    h5 Collections



